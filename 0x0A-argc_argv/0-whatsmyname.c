#include <stdio.h>
/**
  *main - checks the main code
  *@argc:the number of arguments that are going to be passed
  *@argv: pointer to the array of strings
  *Return: Always 0
  */

int main(__attribute__((unused))int argc, char *argv[])
{
	int i;

	i = 0;
	printf("%s\n", argv[i]);
	return (0);
}
